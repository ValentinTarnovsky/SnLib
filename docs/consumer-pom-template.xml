<?xml version="1.0" encoding="UTF-8"?>
<!--
  consumer-pom-template.xml
  pom.xml template for Sn plugins that consume SnLib (standalone hard-depend model).

  HOW com.sn:snlib IS RESOLVED
  1. Publish SnLib to the local Maven repository (.m2):
       mvn install -f <path>/SnLib/pom.xml
  2. The SnLib repo is public on GitHub (ValentinTarnovsky/SnLib). JitPack is
     not configured yet, so the local .m2 install remains the resolution path.
  3. At runtime NOTHING of SnLib is shaded into the consumer: the server loads
     SnLib.jar as a standalone plugin in plugins/ and the consumer declares
     depend: [SnLib] in its plugin.yml. That is why the scope is provided and
     this template does NOT include maven-shade-plugin for the lib. If the
     consumer shades other dependencies of its own, NEVER include com.sn:snlib
     in the shade.

  MINIMAL CONSUMER plugin.yml BLOCK

    name: MyPlugin
    main: com.sn.myplugin.MyPlugin
    version: '${project.version}'   (requires the plugin.yml-only resource filtering below)
    api-version: '1.20'
    depend: [SnLib]
    commands:
      myplugin:
        description: Main command of MyPlugin
    permissions:
      myplugin.admin:
        description: Full administrative access of MyPlugin
        default: op
        children:
          myplugin.admin.reload: true
          myplugin.admin.update: true
      myplugin.admin.reload:
        description: Allows /myplugin reload
        default: op
      myplugin.admin.update:
        description: Receive update notifications of MyPlugin
        default: op

  CONSUMER MAIN CLASS (the only init path: extend SnPlugin)

    package com.sn.myplugin;

    import com.sn.lib.SnApi;
    import com.sn.lib.SnPlugin;
    import com.sn.lib.SnSpec;

    public final class MyPlugin extends SnPlugin {
        protected int requiredApiLevel() { return SnApi.LEVEL; }
        protected SnSpec buildSpec() {
            return SnSpec.builder().config("config.yml").lang().guis().build();
        }
        protected void onInnerEnable() {
            // register commands, guis, items, db, etc. on the Sn context
        }
    }
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.sn</groupId>
    <artifactId>myplugin</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- SnLib: ALWAYS provided. Resolved from the local .m2 (mvn install in SnLib).
             At runtime it is provided by the SnLib.jar plugin installed in plugins/. -->
        <dependency>
            <groupId>com.sn</groupId>
            <artifactId>snlib</artifactId>
            <version>1.11.0</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>1.21.1-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}-${project.version}</finalName>
        <!-- Filter ONLY plugin.yml so ${project.version} resolves to the real version.
             All other resources (config.yml, lang, guis) are copied verbatim, so any
             ${...} tokens they might carry are never touched. -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>plugin.yml</include>
                </includes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <excludes>
                    <exclude>plugin.yml</exclude>
                </excludes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <!-- NO maven-shade-plugin for the lib: SnLib is never shaded into the consumer. -->
        </plugins>
    </build>
</project>
